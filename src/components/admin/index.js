import { default as CommonButtons } from "./CommonButtons";
import { default as Header } from "./Header";
import { default as Pagination } from "./Pagination";
import { default as SideBar } from "./SideBar";
import { default as TableUser } from "./TableUser";

export { CommonButtons, Header, Pagination, SideBar, TableUser };
